# Machine-specific transient state — regenerated per machine, never shared.
session-order

# Machine-specific Linear workspace slug — never committed.
linear-workspace

# TPM-managed plugin checkouts.
plugins/

# Board build inputs. dist/ is committed on purpose: a machine then needs only
# `node` to run the board — no install, no network.
board/node_modules/
